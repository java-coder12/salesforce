public with sharing class ProductManager{

    public Product__c product { get; set;}
    public List<Product__c> productList { get; set; }

    public ProductManager(){
        product=new Product__c();
        productList=[SELECT Id,Name,Product_Id__c,Mfg_Dates__c,Exp_Dates__c FROM Product__c];
    }
    public void saveProduct(){
        insert product;
        product=new Product__c();
        productList=[SELECT Id,Name,Product_Id__c,Mfg_Dates__c,Exp_Dates__c FROM Product__c];
    }
}


<apex:page controller="ProductManager">
    <apex:form >
            <apex:pageBlock title="Product Management System">
                <apex:pageBlockSection columns="1">
                    <apex:inputField value="{!product.name}"/>
                    <apex:inputField value="{!product.Product_Id__c}"/>
                    <apex:inputField value="{!product.Mfg_Dates__c}"/>
                    <apex:inputField value="{!product.Exp_Dates__c}"/>
                </apex:pageBlockSection>
                
                <apex:commandButton value="save" action="{!saveProduct}" rerender="table"/>
            </apex:pageBlock>
            
            <apex:pageBlock title="Product list" id="table">
                <apex:pageBlockTable value="{!productList}" var="p">
                    <apex:column value="{!p.name}"/>
                    <apex:column value="{!p.Product_Id__c}"/>
                    <apex:column value="{!p.Mfg_Dates__c}"/>
                    <apex:column value="{!p.Exp_Dates__c}"/>
                </apex:pageBlockTable>
            </apex:pageBlock>
    </apex:form>
</apex:page>
